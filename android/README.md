# Android App

Jetpack Compose control panel for the local AI video server.

The Android app will remain lightweight: it sends generation jobs to the configurable local server and handles preview, save, and sharing. Heavy AI models are intentionally not bundled into the APK.
